# Задача 04: панель преподавателя — итог

## Сделано

- Прокси `GET` …/teacher-dashboard, типы ответа, страница с KPI, SVG-графиком, Q&A (поиск, «Ещё»), сдачи (Sheet), матрица (`title` на ячейках), состояния loading/error и gate для студента.
- График: SVG-полилиния (без новой зависимости), ось и подписи дат упрощены относительно макета.

## Отклонения от макета

- Заголовки колонок матрицы — порядковый номер этапа (без названий чекпоинтов: в ответе API нет `title` в ячейках).

## Проверка

- Выполнено: `pnpm lint`, `pnpm build` в `frontend/web/` (успешно).
- Ручная проверка: поднять backend с миграциями, задать `BACKEND_ORIGIN` / токен в `.env.local`, войти под преподавателем (например @akozhin), открыть `/teacher`.
