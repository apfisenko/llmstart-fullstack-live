# Скопируйте в .env.local (Next.js подхватывает .env.local).
# Базовый URL backend без завершающего слэша.

BACKEND_ORIGIN=http://127.0.0.1:8000

# Если в backend/.env задан BACKEND_API_CLIENT_TOKEN — задайте то же значение (Bearer для прокси-роутов к API).
# BACKEND_API_CLIENT_TOKEN=
