# Скопировать в .env в корне репозитория или в backend/.env (см. app/config.py).
# BACKEND_API_CLIENT_TOKEN обязателен — общий секрет для Authorization: Bearer (MVP).

BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
LOG_LEVEL=INFO
BACKEND_API_CLIENT_TOKEN=
